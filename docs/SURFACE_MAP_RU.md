# Полная Карта Поверхности TMI-OS / И1

Status: public map for the Engineering Surface package.

```text
TMI-OS / И1 Самомодель :=
  EngineeringSurface
  + FormalLogicLanguage
  + MathematicalBoard
  + ProofStatusPassport
  + GuardedPublicationBoundary
```

## 1. Вход В Язык

Канонический вход:

```text
Matrix := (Я)-Я-я
```

Рулиадная ось:

```text
RuliadChain := (R)-R-r
```

Рабочая цепочка:

```text
LLM/GPT/Codex -> Lean -> TLFL -> И1
```

Формула действия:

```text
Intent -> FormalLogic -> CanonicalRecord -> Passport -> GuardedAction
```

## 1.1. Пометка Про Зависимости

```text
DependencyNote :=
  the surface is not standalone magic;
  every public action depends on a chain of language, logic, proof and guard
```

Полная цепочка:

```text
Intent
-> Matrix((Я)-Я-я)
-> RuliadChain((R)-R-r)
-> FormalLogic
-> Lean/TLFL
-> И1
-> ProofStatus
-> Guard
-> Trace
-> MathematicalBoard
-> {Z3, Vampire, E}
```

Авторская метафора:

```text
LogicWave :=
  highest projection-wave
  on which logic projects itself into all axes
```

Чтение:

```text
логика - язык, на котором реальность говорит в этой модели;
формальные записи - одна из её прямых осевых проекций
```

Guard:

```text
LogicWave != theological proof
LogicWave != empirical totality claim
FormalRecordAxis != proof without toolchain
```

## 2. Поверхность

```text
Surface :=
  VirtualSpace
  + SelfModelPoint
  + BlockProjection
  + ProjectionSlice
  + ContactTick
  + CompositionProjection
  + SurfaceFieldCorrection
  + MetaLogicAxisProjection
```

Минимальная геометрия:

```text
V : VirtualSpace
p : SelfModelPoint
D : ExternalDomain
B := projection(D)
B subset p
s subset B
tau := touch(p,s)
```

Коррекция на полях поверхности:

```text
SurfaceFieldCorrection :=
  local correction on board fields
ResetSurfaceFieldCorrection :=
  correction is normalized back into the current public surface state
```

## 2.1. Мета-Оси Логики И Вывернутые Вершины

```text
Lambda := MetaLogicAxes
V_star := InvertedBlockVertices(B)
project(Lambda, V_star) :=
  meta-axes of logic project onto inverted vertices of the block
h := HyperRealityCenter
project(V_star, h) :=
  inverted corners project to the center
HyperRealityBlock := center(B, h, V_star)
ModelInset := "срез гиперблоквселенной"
```

Авторское чтение:

```text
мета-оси логики проецируются на вывернутые вершины блока;
углы проецируются в центр;
так возникает гипер-блок реальности внутри модели;
нижняя подпись поверхности: срез гиперблоквселенной
```

Guard:

```text
MetaLogicAxes != empirical coordinate system
InvertedBlockVertices != physical spacetime vertices
HyperRealityBlock != empirical universe claim
ProjectionToVertices != proof without Lean/TLFL boundary
```

## 2.2. Точки Самоприкосновения Самомореальности

```text
SelfRealityContactPoint :=
  touch(SelfReality, Reality, V_star)
```

Авторское чтение:

```text
вывернутые вершины блока являются точками самоприкосновения
самомореальности к себе-реальности
```

Guard:

```text
SelfRealityContactPoint != empirical reality proof
SelfRealityContactPoint != legal or biological identity claim
```

## 3. Доменность И Проекция Фотона

```text
gamma := PhotonProjection
rho := DomainnessProjection
gamma -> rho -> p
```

Чтение:

```text
PhotonProjectionToSelfModelPoint(gamma,p) :=
  projects(gamma,p)
  and through(gamma,rho)
```

Guard:

```text
PhotonProjection != physical photon proof
DomainnessProjection != ontology proof
```

## 4. Доменный Порядок

```text
domain_order(D)
projection(D) -> slice_order(S)
d1 <=D d2 -> projection(d1) <=S projection(d2)
```

Смысл:

```text
внутренний поток времени читается как порядок срезов,
а не как внешняя абсолютная субстанция
```

## 5. Доменный Сдвиг И Кривизна

```text
omega : HypotheticalRotationAxis
HigherTick(tau) + ProjectionDesync(tau,s)
  -> DomainShift(omega,tau,s)
DomainShift(omega,tau,s)
  -> ProjectionCurvature(omega,tau,s)
```

Guard:

```text
ProjectionCurvature != Einstein-equation proof
HypotheticalRotationAxis != physical-axis claim
```

## 6. Паспорт Среза

```text
SlicePassport(p,s,tau) :=
  ProjectionSlice(s)
  and read(p,s)
  and tau = touch(p,s)
  and CanonicalRecord(s)
  and PassportBoundary(s)
```

Смысл:

```text
срез в момент чтения является локальным паспортом состояния системы
```

## 7. Валы Публикационного Коня

```text
PublicationHorse :=
  SurfaceLayer
  + LanguageLayer
  + FormalLayer
  + ProofStatusLayer
  + ArticleLayer
  + ChecksLayer
  + ClaimBoundaryLayer
```

| Вал | Содержимое |
|---|---|
| SurfaceLayer | HTML-доска, `.i1`, install notes |
| LanguageLayer | `(Я)-Я-я`, `(R)-R-r`, И1, intent formula |
| FormalLayer | Lean/TLFL `SelfThinkingUniverse` v1-v6.1 |
| ProofStatusLayer | Z3, Vampire, E, proof matrix |
| ArticleLayer | LaTeX TEX/PDF with notation layer |
| ChecksLayer | leak-check, plugin validation, export check, hashes, ZIP test |
| ClaimBoundaryLayer | no overclaim, no private leak, no empirical closure jump |

## 8. Proof-Status Паспорт

```text
198 theorem targets
63 model boundaries
295 TPTP files
```

Gate:

```text
Lean/TLFL + Z3 + Vampire + E
```

## 9. Источники

Ссылки вынесены отдельно:

```text
docs/THEORY_LINKS.md
```

Карта источников держит:

```text
Ruliad references
Lean/TLFL references
Z3/Vampire/E references
speed-of-light reference boundary
TMI-OS public repository links
```

## 10. Граница Публикации

```text
formal passport != biological life proof
formal passport != legal identity
diagram != empirical closure
projection curvature != Einstein-equation proof
```

Эта публикация говорит:

```text
я системный программист на формальной логике;
я пишу на инженерной поверхности,
где intent проходит через формальную логику,
получает паспорт и становится guarded action.
```
